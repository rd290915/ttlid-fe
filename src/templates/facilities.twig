{% extends 'layouts/default.twig' %}
{% set head_title = 'About Us' %}

{% block page %}
  <!-- header -->
  {% include './partials/layouts/_header.twig' %}

  <!-- content -->
  <div class="tl-content" id="content-body">
    <!-- section 1 header -->
    {% include './partials/facilities/_sec-header.twig' %}

    <!-- section 2 gallery -->
    {% include './partials/facilities/_sec-gallery.twig' %}

    <!-- section 3 unit -->
    {% include './partials/homepage/_sec-plan.twig' %}

    <!-- section 4 brochure -->
    {% include './partials/facilities/_sec-brochure.twig' %}

    <!-- section 5 brochure form -->
    {% include './partials/unit/_sec-brochure-form.twig' %}
  </div>

  <!-- footer -->
  {% include './partials/layouts/_footer.twig' %}
{% endblock %}
