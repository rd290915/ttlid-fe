{% extends 'layouts/default.twig' %}
{% set head_title = 'About Us' %}

{% block page %}
  <!-- header -->
  {% include './partials/layouts/_header.twig' %}

  <!-- content -->
  <div class="tl-content" id="content-body">
    <!-- section 1 header -->
    {% include './partials/surrounding/_sec-header.twig' %}

    <!-- section 2 marker -->
    {% include './partials/surrounding/_sec-marker.twig' %}

    <!-- section 3 our -->
    {% include './partials/surrounding/_sec-our.twig' %}

    <!-- section 4 future -->
    {% include './partials/surrounding/_sec-future.twig' %}

    <!-- section 5 brochure -->
    {% include './partials/surrounding/_sec-brochure.twig' %}

    <!-- section 6 brochure form -->
    {% include './partials/unit/_sec-brochure-form.twig' %}
  </div>

  <!-- footer -->
  {% include './partials/layouts/_footer.twig' %}
{% endblock %}
